# PX4_FIRMWARE

Firmware archive written by AIRFRAME_DESIGNER: one folder per flash under `versions/` (image, full parameter set, exported parameters, airframe, module sources). Restore any of them from the app's Versions tab.
